"use client"

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import { Badge } from "@/components/ui/badge"
import { Progress } from "@/components/ui/progress"
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion"
import type { AnalysisModule } from "@/lib/mock-data"

interface ModuleResultsProps {
  modules: AnalysisModule[]
}

export default function ModuleResults({ modules }: ModuleResultsProps) {
  return (
    <div className="space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>ML Module Results</CardTitle>
          <CardDescription>Detailed results from each analysis module</CardDescription>
        </CardHeader>
        <CardContent>
          <Accordion type="single" collapsible className="w-full">
            {modules.map((module, index) => (
              <AccordionItem key={module.name} value={`module-${index}`}>
                <AccordionTrigger className="hover:no-underline">
                  <div className="flex items-center justify-between w-full pr-4">
                    <span>{module.name}</span>
                    <Badge variant={module.score > 75 ? "destructive" : "outline"} className="ml-2">
                      {module.score}%
                    </Badge>
                  </div>
                </AccordionTrigger>
                <AccordionContent>
                  <div className="space-y-4 pt-2">
                    <p className="text-sm text-muted-foreground">{module.description}</p>

                    <div className="space-y-2">
                      <div className="flex justify-between text-sm">
                        <span>Detection Confidence</span>
                        <span className="font-medium">{module.score}%</span>
                      </div>
                      <Progress value={module.score} className="h-2" />
                    </div>

                    <div className="space-y-2">
                      <h4 className="text-sm font-medium">Key Insights:</h4>
                      <ul className="list-disc pl-5 text-sm space-y-1">
                        {module.insights.map((insight, i) => (
                          <li key={`insight-${i}`}>{insight}</li>
                        ))}
                      </ul>
                    </div>

                    <div className="pt-2">
                      <p className="text-sm font-medium">Flagged {module.flaggedReviews.length} reviews</p>
                    </div>
                  </div>
                </AccordionContent>
              </AccordionItem>
            ))}
          </Accordion>
        </CardContent>
      </Card>

      <Card>
        <CardHeader>
          <CardTitle>Technical Details</CardTitle>
          <CardDescription>Information about the ML models used in the analysis</CardDescription>
        </CardHeader>
        <CardContent>
          <div className="space-y-6">
            <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
              <div className="p-4 border rounded-lg">
                <h3 className="font-medium mb-2">Language Stylometry</h3>
                <p className="text-sm text-muted-foreground mb-2">
                  Uses SVM and BERT models to detect patterns in writing style typical of bots and AI-generated content.
                </p>
                <Badge variant="outline">SVM</Badge>
                <Badge variant="outline" className="ml-2">
                  BERT
                </Badge>
              </div>

              <div className="p-4 border rounded-lg">
                <h3 className="font-medium mb-2">Sentiment & Emotion Detection</h3>
                <p className="text-sm text-muted-foreground mb-2">
                  Uses VADER and RoBERTa to classify emotional tone and detect unnatural sentiment patterns.
                </p>
                <Badge variant="outline">VADER</Badge>
                <Badge variant="outline" className="ml-2">
                  RoBERTa
                </Badge>
              </div>

              <div className="p-4 border rounded-lg">
                <h3 className="font-medium mb-2">GAN Detection</h3>
                <p className="text-sm text-muted-foreground mb-2">
                  Specialized models to detect content generated by large language models like ChatGPT.
                </p>
                <Badge variant="outline">GPT-2 Output Detector</Badge>
                <Badge variant="outline" className="ml-2">
                  RoBERTa
                </Badge>
              </div>

              <div className="p-4 border rounded-lg">
                <h3 className="font-medium mb-2">Network Graph Detection</h3>
                <p className="text-sm text-muted-foreground mb-2">
                  Graph-based algorithms to detect coordinated fake review networks and spam campaigns.
                </p>
                <Badge variant="outline">NetworkX</Badge>
                <Badge variant="outline" className="ml-2">
                  Community Detection
                </Badge>
              </div>
            </div>
          </div>
        </CardContent>
      </Card>
    </div>
  )
}
